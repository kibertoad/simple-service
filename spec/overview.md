# main-service — Requirements

> Generated, prescriptive requirements for this service (what MUST be
> true). Read this first. `rules.md` lists cross-cutting invariants;
> `features/*.feature` are the acceptance scenarios your work must satisfy.

An in-memory multi-entity CRUD service exposing REST endpoints for Users, Organizations, Buildings, and Cars, with system-generated UUID identifiers, transient in-memory storage, field-level validation, and basic error handling.

_No requirements captured yet._
