{
  "name": "Car Management",
  "summary": "Capabilities for typical CRUD operations on Car resources using a transient in-memory store.",
  "requirements": [
    {
      "id": "REQ-020",
      "title": "Car CRUD endpoints",
      "statement": "The system SHALL expose REST endpoints to create, retrieve, list, update, and delete Car resources, using an in-memory store.",
      "kind": "functional",
      "priority": "must",
      "sourceBlockIds": [
        "task_1ae9bb19c6a1"
      ],
      "acceptance": [
        {
          "id": "REQ-020-AC-01",
          "given": "a valid create request for a Car",
          "when": "the request is processed",
          "outcome": "the Car is persisted with a system-generated unique identifier and returned"
        },
        {
          "id": "REQ-020-AC-02",
          "given": "a stored Car and a valid identifier",
          "when": "the retrieve request is processed",
          "outcome": "the system returns the Car record"
        },
        {
          "id": "REQ-020-AC-03",
          "given": "a Car identifier that does not exist",
          "when": "the retrieve request is processed",
          "outcome": "the system performs error handling indicating the resource was not found"
        },
        {
          "id": "REQ-020-AC-04",
          "given": "one or more stored Cars and a list request",
          "when": "the request is processed",
          "outcome": "the system returns a collection of Car records with cursor-based pagination"
        },
        {
          "id": "REQ-020-AC-05",
          "given": "a stored Car and a valid full-update request",
          "when": "the request is processed",
          "outcome": "the system replaces the stored Car fields and returns the updated record"
        },
        {
          "id": "REQ-020-AC-06",
          "given": "a non-existent Car identifier on update",
          "when": "the request is processed",
          "outcome": "the system performs error handling indicating the resource was not found"
        },
        {
          "id": "REQ-020-AC-07",
          "given": "a stored Car identifier",
          "when": "the delete request is processed",
          "outcome": "the system permanently removes the Car from the in-memory store"
        },
        {
          "id": "REQ-020-AC-08",
          "given": "a non-existent Car identifier on delete",
          "when": "the request is processed",
          "outcome": "the system performs error handling indicating the resource was not found"
        }
      ]
    },
    {
      "id": "REQ-021",
      "title": "Car identifier and representation",
      "statement": "The system SHALL generate a unique identifier for each Car upon creation and represent a Car with a fixed set of fields.",
      "kind": "functional",
      "priority": "must",
      "sourceBlockIds": [
        "task_1ae9bb19c6a1"
      ],
      "acceptance": [
        {
          "id": "REQ-021-AC-01",
          "given": "a Car creation request",
          "when": "the Car is persisted",
          "outcome": "the id value is a valid, system-generated identifier"
        },
        {
          "id": "REQ-021-AC-02",
          "given": "two independent Car creation requests",
          "when": "both Cars are persisted",
          "outcome": "each Car is assigned a distinct id"
        },
        {
          "id": "REQ-021-AC-03",
          "given": "any persisted Car",
          "when": "the record is inspected",
          "outcome": "it contains the expected fixed fields and no extra computed fields"
        }
      ]
    },
    {
      "id": "REQ-022",
      "title": "Car in-memory persistence",
      "statement": "The system SHALL persist all Car data in memory only, with an empty store on startup and no data survival across restarts.",
      "kind": "nonfunctional",
      "priority": "must",
      "sourceBlockIds": [
        "task_1ae9bb19c6a1"
      ],
      "acceptance": [
        {
          "id": "REQ-022-AC-01",
          "given": "a Car has been persisted",
          "when": "the process is restarted",
          "outcome": "no Car data from the previous process lifecycle remains"
        }
      ]
    }
  ],
  "rules": [
    {
      "id": "RULE-023",
      "rule": "A Car entity has a fixed set of fields and a system-generated unique identifier, and persists only in memory for the lifetime of the process.",
      "rationale": "Ensures a predictable transient model for simple CRUD.",
      "sourceBlockIds": [
        "task_1ae9bb19c6a1"
      ]
    }
  ]
}
